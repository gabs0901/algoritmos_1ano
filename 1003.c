// 1003.c: Solução do problema 1003 (beecrowd)
#include <stdio.h>

int main (){
    //declaracao de variaveis    
    int a, b, s; // a e b sao entradas e s é a soma 
    
    //leitura das variaveis
    scanf("%d %d", &a, &b);

    //calculo da soma
    s = a + b;

    //imprimir o resultado
    printf("SOMA = %d\n", s);

    return 0;
}
